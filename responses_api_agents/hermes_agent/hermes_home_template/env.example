# Hermes Agent secrets template.
# Provider API keys and other secrets are injected by the pipeline overlay
# (typically from cluster_config or sbatch environment) — do not commit
# real credentials here.
