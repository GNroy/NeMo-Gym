# Memory

This file is the agent's persistent long-term memory.  The curator step
appends durable lessons here at the end of each run, scoped to what is
generally useful across tasks (best practices, recurring pitfalls, tool
usage patterns).  Per-task scratch belongs in the conversation, not here.
